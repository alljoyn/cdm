{
    "cdm_tcl": {
        "bin_dir": "%% This should point to the cdm_tcl dist/bin dir %%",
        "mount_dir": "%% This should point to where ever the state files should be written to %%"
    },
    "cdm": {
        "bin_dir": "%% This should point to the cdm dist/bin dir %%",
        "mount_dir": "%% This should point to where ever the state files should be written to %%"
    }
}
